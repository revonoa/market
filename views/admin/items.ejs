<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>관리자 | 게시물 관리</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <div class="container">
        <nav class="header">
            <a href="/">← 메인</a>
            <h1 style="margin:0 0 0 12px">관리자 대시보드</h1>
        </nav>

        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>제목</th>
                    <th>가격</th>
                    <th>판매자</th>
                    <th>상태</th>
                    <th>생성일</th>
                    <th>관리</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach(it=> { %>
                    <tr>
                        <td>
                            <%= it.id %>
                        </td>
                        <td><a href="/items/<%= it.id %>" target="_blank">
                                <%= it.title %>
                            </a></td>
                        <td>
                            <%= it.price.toLocaleString() %>원
                        </td>
                        <td>
                            <%= it.seller %>
                        </td>
                        <td><span class="badge <%= it.status %>">
                                <%= it.status %>
                            </span></td>
                        <td>
                            <%= it.created_at.toISOString ? it.created_at.toISOString().slice(0,19).replace('T',' ') : it.created_at %></td>
            <td class="row">
              <form method="post" action="/admin/items/<%= it.id %>/status">
                <select class="select" name="status">
                  <option value="on_sale"  <%= it.status===' on_sale'?'selected':'' %>>판매중</option>
                                <option value="reserved" <%=it.status==='reserved' ?'selected':'' %>>예약중</option>
                                <option value="sold_out" <%=it.status==='sold_out' ?'selected':'' %>>판매완료</option>
                                </select>
                                <button class="btn" type="submit">변경</button>
                                </form>
                                <form method="post" action="/admin/items/<%= it.id %>?_method=DELETE"
                                    onsubmit="return confirm('삭제할까요?')">
                                    <button class="btn danger" type="submit">삭제</button>
                                </form>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>